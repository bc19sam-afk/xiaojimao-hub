# 小鸡毛账号收集系统 · 实现路线图 v3

> 依据《需求文档.md》v3 + codex 两轮工程 review + Claude 自核补充。只排**实现顺序与工程/运维纪律**，具体改法在各阶段简报里定。
> v3 相对 v2 的改动：P0 拆「先调研后基建」、标注优先级接口真空、mock 每阶段同步、风险披露定为不做。

---

## 部署形态（首版钉死）

- **最终部署到服务器**。首版形态：**单台服务器 + 单个 Node 应用实例 + SQLite（持久化磁盘）+ 单 worker**。
- SQLite 首版可用，但 **「单机单实例」是硬边界**：一旦要 Docker 多副本 / 负载均衡 / 多机 / serverless，须改 PostgreSQL 并把 worker 独立进程化（未来演进，非首版）。
- **worker 单实例**：worker 随应用启动（`instrumentation.ts`）、进程内 `running` 锁跨不了实例；多实例会同时巡检/启用/删 CPA 号，DB 幂等**挡不住 CPA 外部副作用**。首版靠"单实例"保证；未来多实例须给 worker 加 DB 锁/租约。

## 0. 执行纪律（从 P0 起全程）

- 分支 + PR，不直推 main；每份 PR：build 绿 + CI 绿 + 相关测试 + `git diff` 边界符合简报 + 总指挥 review 后合并。
- 一份简报一个**全新执行对话**。
- **mock 同步**：每阶段 mock client 同步支持新增能力（优先级/复检/新状态机等），保证 `MOCK=true` 下可全链路验证。
- **迁移单次执行**：不由应用实例启动时自动跑；部署时独立步骤——获取迁移锁 → 一致性备份 → 跑待运行迁移 → 校验 schema 版本 → 再启动应用。
- **破坏性迁移**（删列/重建表）单独 PR + 人工确认；优先向后兼容「先加后删」，旧版本代码能暂时跑在新 schema 上；回滚 = 回滚代码 + 恢复迁移前备份（不做自动 down-migration）。
- schema 改动一律走迁移框架，**禁止删库重建**。

## P0 · 工程安全垫（先行，不碰业务逻辑）

**分两步：先调研（P0-A）摸清外部不确定性，结论回填后再基建（P0-B）。**

### P0-A · 外部能力探测（纯调研，最先，不写业务码）
对真实 cpamp **只读**试 API，摸清四个关键假设：
1. 能否**设账号优先级**——当前 `CpaClient` 接口（`cpa.ts:67`）**完全没有** setPriority；若 cpamp 也不支持，「默认 10 优先级」需求要**回炉/降级，先报用户**。
2. 能否**打来源标记/备注**（auth-files 上传能否带 tag / 除 `provider-accountId.json` 命名外的元数据）。
3. **禁用是否带原因**——能否区分「管理员手动禁用」vs 失效/限流（关系到 §3.3 判定表能否落地）。
4. **claude/grok 的 auth-files 是否返回稳定 `account_id`**——决定稳定 ID 规则出口。

产出：探测结论文档；据结论**封死 claude/grok 稳定 ID 规则**（无稳定 ID → 暂停该 provider 或人工录入可永久复用、可验证的 canonical ID，**绝不无标识放行**）；cpamp 不支持的能力**回填路线图/需求**。

### P0-B · 工程基建（在 P0-A 结论之上）
1. **版本化迁移框架**：`schema_version`；**独立可执行** migrate + 迁移锁；**重建表模式**改约束（SQLite 不能 ALTER 删/换 UNIQUE）；现有 schema 固化为 **baseline 001**，全新库与旧库走**同一迁移链**（`CREATE TABLE IF NOT EXISTS` 不再单独维护 schema）；无版本旧库认定规则写死。
2. **WAL 安全一致性备份**：`VACUUM INTO` / 在线备份 / checkpoint 后复制（裸 copy 会漏 `app.db-wal`）；持久化目录 + 异机/对象存储 + 保留数量/权限 + **至少验证一次能恢复**。
3. **CI（GitHub Actions）**：build + 测试 + 两条迁移测试（① 空库跑全迁移→schema 正确 ② 旧库升级→数据不丢、约束与积分正确）。
4. **最小回归测试**：`node --test`（Node 内置 runner，不引额外依赖）锁核心不变量——去重、发分幂等、原子扣分、状态转移。

> **校准**：当前库业务数据为 0（仅 7 条发分规则 + 4 个商品**种子**，可自动重生成），**此刻删库无损**。迁移框架是为**生产/未来真实数据**建纪律，非当前抢救。

## P1 · 数据模型地基（三份，在 P0 安全垫上；依赖 P0-A 稳定 ID 规则已封死）

- **最小 `app_config` 数据层**提前（KV，服务 P2 的 T/优先级/快照；UI 留 P4）。
- **「待人工复核」最小处理入口**随 P1 提供（不等 P4）——让 P2 产生的人工任务有出口。
- **P1a 唯一键**：迁移到 `(provider, account_id)` 复合唯一（重建表模式）；contributions 行永不删除（＝永久账本）。**前置守卫**：P0-B-1 的 openDb 目前启动即自动 migrate（对幂等 baseline 无害）；加破坏性迁移前须改为「生产只校验 schema 版本、有未跑迁移拒绝启动」，对齐「迁移单次执行」纪律。
- **P1b 终身账本语义**：发分 / 已发分标记锚定 `(provider, account_id)`；落地 P0-A 的稳定 ID 规则。（P1b-1 已落地：claude 读 `account` 字段、兜底仅限 claude、终身账本不变量测试。）
- **P1b-2 grok 稳定 ID + 诚实拒绝（已合并 PR #11）**：grok 稳定 ID＝`sub`（扒 CLIProxyAPI xai 源码实锤，三家字段齐：codex=chatgpt_account_id / claude=account / grok=sub，各自 provider 划界兜底）；recordIngest 拆「认不出身份」（诚实提示重试）vs「真重复」（这个号交过了）；findNew 识别「重交已删号」带回身份报"交过了"且 isolate 零副作用。原设想的 needs_review+人工录入 canonical ID 子系统**不再需要**（三家都有稳定字段，认不出只剩残缺号→拒绝即可）；人工任务入口整体留 P2。
- **P1b-3 修 findNew 抢注（已合并 PR #9）**：授权回调前拍 auth-files 快照，findNew 只认快照外的新文件，挡住抢注 cpamp 池中既有号（官方池号！）。redirect 流程（claude/codex）单请求内快照已落地。
- **P1b-4 OAuth 快照跨请求持久化（已合并 PR #10）**：快照 startOAuth 拍、按 state 持久化（migration 003 新表 oauth_snapshots，TTL 1h+用完删）；finishOAuth retry 复用不孤立、device/checkOAuth 也能读挡抢注；快照缺失走 **fail-closed** 拒绝（不静默降级空 before，codex xhigh 补抓）。
- **P1b-5 并发授权互认窗口（⚠️ 真实对接前评估）**：快照移到 startOAuth 后，两用户同 provider 授权会话重叠时 B 可能认领 A 刚落的号（窗口从毫秒级扩到授权会话时长）。彻底解法：探测 cpamp 回调/状态响应能否按 state 关联本次文件（能→响应关联替代 findNew 猜测；不能→同 provider 授权互斥/串行化）（codex xhigh 于 PR #10 发现）。
- **P1c 复合 canonical key 全链路接线**：所有去重 / 发分幂等 / 查询 / 索引统一用 `(provider, account_id)`；覆盖**整个收号链路**——修 `collect.ts:65` 混集合、`cpa.ts:209` `findNew()` 未按 provider 过滤，否则 DB 改复合键后应用层仍会误判重复 / 拿错并发落入的号。

## P2 · 按量计量闭环（v4 换引擎；先于 P3）

> **v4 转向（2026-07-20）**：考察期整个取消，发分改「按 cpamp 每日调用量结算（按次×单价）」。**策略＝改造不回滚**——已合并的 P2a-1/a-2/b（migration 004-006、observations 表、考察快照列、暂停列、6 态机、故障顺延）**迁移链不可逆故保留**，发分逻辑重做；废弃的表/列**留库不删**（SQLite 删列麻烦、留着无害），代码断引用并标注废弃。老 P2 的基建价值（迁移框架实战、cpamp inspect 对接、双审沉淀）留用，不是白干。
> **v4 前已合并（老考察模型，将被 R1/R2 改造）**：P2a-1 观测事件表+快照列（PR #12）、P2a-2 6 态机+考察闭环（PR #13）、P2b codex 巡检+故障顺延（PR #14）。

- **P2-R1（已合并 PR #15，破坏性）**：6 态→5 态（migration 007：observing·granted→pooled；failed 按来源分流——进过池→stopped 锁键、未入池→删行释放可重交；observing+已记 hard_fail→stopped）；processPending 拆考察期→首检入池（ok/配额类 retry 入池；非配额 retry 留首检；reject 先删文件成功才 CAS 删行退回；reauth→needs_review）；UI 5 态、排行榜按 pooled 号数（按累计积分留 R2/R3）；废弃表/列/函数留库断引用标 @deprecated。codex xhigh 两轮 7 条：5 修 2 立项（claude/grok 可用性探测、退回可见提示→并入 R3）。
- **P2-R2（已合并 PR #16）**：发分引擎落地——`getDailyUsage`（mock + real 骨架聚合 /v0/management/usage）、`usage_rates` 按次单价（migration 008 两表）、`daily_settlements` 一号一日一笔、`settleDailyUsage`（按日驱动一天一次 + 日切延迟 10min、只结已过完自然日、资格 pooled+stopped 补结欠薪、settleAndAward 同事务双 UNIQUE 幂等）。codex xhigh 4 条全采纳（节流/label 过滤降级为 pooled 索引权威/stopped 欠薪/账本同事务）。残留：real getDailyUsage 字段+hub label 格式待真实对接核对；「来源标记写入」仍在残项。MOCK 端到端已演示。
- **P2-R3（已合并 PR #18，非破坏）**：号存活巡检（pooled 期 codex inspect / claude·grok 文件在否判失效→stopped·reauth→needs_review，5min 节流、空清单保护防 glitch 误停整池）+ dashboard 每号「在用/已失效」+ 累计积分 + 积分明细「X 号 M 月 D 日用量结算」文案 + 首检退回可见提示。**结算资格重构**：从「看当前 verify_status」改为 migration 010 的 `pooled_at`（入过池才结、`transitionToPool` 原子进池写入），消除首检 reauth（从没入池）vs 巡检 reauth（入过池）歧义。codex 两轮复审共 10 条：第 1 轮 4 条（pooled_at 资格替糙修/空清单保护/巡检节流/清退回），第 2 轮 4 条修（原子进池/回填 `created_at` 安全下界/入池当日不结 `<=`/clearRejections 限本人）+ ②驳回·⑤⑥⑦⑨ defer；GitHub bot 2 条采纳（pooled 态 30s 轮询/reauth 欠薪由 pooled_at 覆盖）。真实对接前残项见 task #23（离池上界 stopped_at）/#24（真空 N 轮判死）。测试 109 全绿、CI 绿。
- **入池优先级 + 来源标记**（原 P2c 残项，仍要）：贡献号入池设高优先级（数字越大越先烧＝号主多用量多积分）+ cpamp 来源标记。入池优先级接口层仍缺，须从 `CpaClient` 接口→mock→real→调用点整条新建（依赖 P0-A 确认 cpamp 可设优先级）。可并入 R1/R3。

依赖：R1（状态机）→ R2（计量发分闭环）→ R3（存活展示）。**P2 v4 三轮 R1-R3 已全部合并，按量计量闭环在 MOCK 端到端打通；真实 cpamp 对接留待专项（残项见 task #13/#18/#23/#24 及各 R 残留）。下一步 P3 商店 CDK+LDC。**

## P3 · 商店 CDK + LDC

CDK 库存表（可用/已发放/作废）+ 批量导入去重、兑换单事务（查余额→查限购→占 CDK→扣分→写记录）、售罄/限购拦截、CDK 找回。**LDC 类＝CDK 式发码 + 每日限量**（默认 2000 面额/日、按日重置额度、当日额度用尽＝今日抢完不扣分）。含自身数据层，不依赖 P4。纯站内（码是后台预导入串、不依赖 cpamp），端到端真做。拆两轮：

- **P3-R1（已合并 PR #19，非破坏）**：CDK 履约闭环。migration 011（cdk_codes 表 available/issued/void + face_value 预留 + redeem_items 加 fulfillment/per_user_limit）；批量导入去重（importCdkCodes + parseCdkCodes 换行/逗号/空白分隔 + 最小 admin API）；**兑换单事务 performRedeem**（单 BEGIN IMMEDIATE：幂等回放→限购→CAS 占码→扣分→写记录，任一失败全回滚不扣分）；售罄/超限购拦截；CDK 找回限本人（不进日志/排行榜/他人接口）；AdminPanel 加履约类型+限购入口。**顺带修 2 既有 bug**：扣分与写记录非同事务、兑换 ref 随机 hex 致重复点击重复扣分（改确定性幂等键 token/短窗兜底）。codex 两轮（4+3）+ GitHub bot 3 条复审：修 CDK 激活入口/成功不误报双花/空白分隔/透传值域/成功即给码；defer 失败回放（三叠加边缘）·auto 桶根治（无客户端稳定键，见 task）。测试 123 全绿。
- **P3-R2（已合并 PR #20，非破坏）**：LDC 每日限量。批级面额导入（LDC 商品强制正整数 faceValue、非 LDC 恒 null）；**额度判定在 performRedeem 事务内**（占码后含本码的当日已发面额和 > `app_config['ldc_daily_quota']`（缺省 2000）→ 回滚「今日已抢完」不扣分；边界 ≤ 恰好等于可发；幂等回放不双计）；按服务器本地自然日重置（localDayBounds 与 dayStr 同口径）；store `soldOutToday` 布尔 + 前端置灰 + 60s 慢轮询跨午夜自动恢复；admin ldc-quota 读改 API。codex 复审 5 条全采纳：**额度统计与 kind 解耦**（只看 face_value 非空，防改 kind/删商品释放额度）、LDC 占无面额码拦「配置异常」（防错序配置绕额度）、faceValue/quota 只认 number 安全整数（防宽转+ERR_OUT_OF_RANGE 500）。测试 135 全绿。无新 migration（face_value 列 R1 已预留）。**P3 两轮全合并，商店 CDK+LDC 端到端闭环（纯站内、无真实对接尾巴）。下一步 P4 后台配置化+审计。**

## P4 · 后台配置化 + 审计（收口）

完整后台 UI：信任门槛/限身份、**折算规则（按次单价/分档）+ 结算参数 + 优先级**编辑、商品限购/**LDC 每日额度**、CDK 导入 UI、贡献&**每日结算记录**&兑换记录查看、审计留痕（操作人/时间/旧值/新值）、人工重试/终止（在 P1 最小入口上补完整）。拆三轮：

- **P4-R1（已合并 PR #21，非破坏）**：审计地基 + 身份透传 + 补 CDK/LDC 配置 UI。**身份透传**：纯决策 `resolveAdminActor` 抽 `lib/admin-actor.ts`（不 import next/headers＝node --test 可直测），Actor＝密码会话→匿名「管理员(密码会话)」/ linux.do 管理员→真实 id+用户名，两入口都在时密码优先（与旧 isAdmin 短路序一致）；`getAdminActor()` 收 Next 运行时读取，isAdmin 改判 actor 非空（语义不变）。**审计**：migration 012 `audit_log`（actor/action/target/old/new/时间）+ `db.recordAudit/listAudit`（倒序分页钳 limit）；`lib/audit.ts` 纯构造器集中脱敏，接全部现有写入点（point_rules / redeem_items upsert+delete、cdk 导入、ldc-quota），紧邻主操作非同事务（单机单 worker 足够，异常上抛不吞）。**§8 脱敏铁律**：auditCdkImport 签名不收 codes＝结构上不可能泄码，只记「导入 N/跳过 M（面额 F）+库存 before→after」；测试扫全表断言无码原文。补 AdminPanel：CDK 导入区块（选项+贴码+面额+库存概览，不回显码）、LDC 每日额度控件、审计日志只读区块 + `/api/admin/audit` GET。测试 147 全绿（+12）。
- **P4-R2（已合并 PR #22，非破坏）**：后台配置化三块，全走 R1 审计地基、**无新 migration**。**A 折算规则** usage_rates 配置化：镜像 point_rules 全链路（db CRUD `listUsageRates/upsertUsageRate/deleteUsageRate` + `/api/admin/usage-rates` GET/PUT/DELETE + AdminPanel RateRow），provider/plan 小写规整与 ratePerCall 查表口径一致；**单价=小数**（`Number.isFinite`≥0，禁 isSafeInteger，0.1 保真直测）。**B 信任门槛&限身份**（§1）：app_config `min_trust_level`（缺省回落 env.MIN_TRUST_LEVEL）+ `trust_gate_enabled` 开关（缺省开；关＝登录即可不限等级），登录回调改读 db，门槛只判此后登录、不失效已有会话。**C 结算参数**（§3.3）：`settle_grace_minutes` 配置化（缺省仍 10min、钳 [0,1439]），settle.ts 每轮读取；时区随服务器不可配。三块写入全 recordAudit（usage_rate.upsert/delete、trust_gate.set、settle_param.set）。codex xhigh 复审 3 条全采纳：**usage_rates 一次性播种**（marker 键 `usage_rates_seeded`，防「管理员删空停发→重启回种恢复发分」）、**存量行 provider/plan 禁改**（upsert 按键、改键=插新行双档并行计价；改档=先删再增）、**单价上界两层**（路由 ≤1e6 + settle 侧 `Number.isSafeInteger(points)` 防御闸：非法不结不发、不记 0 分凑数防 hasSettled 吞欠薪、修好单价下轮自愈，含自愈直测）。测试 163 全绿（+16）。
- **P4-R3（已合并 PR #23，非破坏，P4 收官）**：数据查看 + 人工复核处理，全走 R1 审计地基、**无新 migration**。**一 数据查看（§6.146）**：管理侧全局分页只读三查询器（倒序、limit∈[1,200] 钳制仿 listAudit、SELECT 明确列杜绝敏感列外泄）——`listContributionsAdmin`（points=每行 contributionPoints 累计发分；**脱敏无 email/reward_code**）/`listSettlementsAdmin`（LEFT JOIN 取归属人、孤儿行 null/空串兜底）/`listRedemptionsAdmin`（子查询取 username；**🔴§8 绝不 SELECT result＝CDK 码**，测试扫返回断言无码）+ 三 GET-only 路由（getAdminActor 鉴权、纯只读不 recordAudit）+ AdminPanel 三只读表格区块。**二 人工复核（§7.4）**：needs_review 死胡同补人工出口——`retryReview`/`terminateReview`（→stopped 保留行审计）均走 transition CAS；**🔴§7.4 幂等：只改 verify_status、不碰 daily_settlements/point_ledger**（测试断言前后 settlements/ledger/balance 无变）；`auditContributionReview` 留痕 + review 路由（GET 队列/POST 转成功才审计、CAS 返 false=态已变不审计）+ AdminPanel 队列区块。codex xhigh 复审 3 条全采纳：**P1 入过池 retry 按 pooled_at 分叉**（needs_review 两类：从没入池→回首检 submitted 照旧；**入过池（巡检转出）→直接回池 pooled**——回首检会被 transitionToPool 覆写 pooled_at＝结算下界后移历史欠薪永久跳过、且首检 reject 删行释放唯一键违反 §2.4；直回池 pooled_at 不动欠薪照补、号仍坏由巡检再转出，审计 new 随真实去向；G1 直测「入池日后、重试日前」欠薪照补）、**复核成功后刷新贡献表**（防同页两态矛盾）、**offset 归一 Number.isFinite 否则回落 0**（`?offset=Infinity` 穿过 `||0` 与 max(0,…) 到 SQLite OFFSET 绑定抛 500；四查询器含 R1 listAudit 同款一并修）。测试 176 全绿（+13）。**P4 三轮全合并收官（配置化+审计+数据查看+人工复核）。下一步 P5 展示+收尾。**入池优先级全程 defer（依赖 cpamp 真实对接）。

## P5 · 展示 + 收尾

排行榜按累计积分、积分明细页（含每日用量结算笔）、dashboard 每号存活/失效+累计积分、折算规则种子对齐 doc、CPA 报错不透传、admin 登录限流、（原「文案中性化」并入）。

## P6 · 服务器部署与运维

HTTPS / 反向代理、Linux.do OAuth 回调域名、环境变量与密钥管理、`data` 持久化卷（Docker 时挂载，不进镜像/Git，权限限应用用户）、单实例 worker 保证、部署迁移流程（锁+备份+校验+启动）、异机备份、日志轮转、健康检查、恢复演练。

## 依赖

```
P0-A（探测）→ P0-B（基建）→ P1(app_config, 人工复核入口, a, b, c) → P2[老 a/a/b 已合并，v4 改造 R1→R2→R3] → P3(CDK+LDC) → P4 → P5 → P6
```

## 产品决策（已定）

- **风险披露：不做**——不做披露页、不加知情提示、维持不允许主动撤回（用户 2026-07-15 决）。
