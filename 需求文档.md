# 小鸡毛账号收集系统 · 需求文档（定稿 v3）

> 日期：2026-07-15　｜　状态：需求锁定。**v3 改动**：去重改为「一个号一辈子只能提交一次、掉了永久作废」，移除 v2 的「可重交」整条线（涉 §2.4 / §3.1 / §3.2 / §4 / §6）。
> 本文只描述**需求**，不涉及当前代码进度与实现方案。

---

## 0. 定位与边界

- **一句话**：贡献者交出自己的 AI 订阅账号 → 进我的 CPA(cpamp) 号池 → 自动巡检 → 发积分 → 积分兑换 CDK。
- **边界（关键）**：号池的长期监控/管理是**另一个站**的事。本站只做**贡献台 + 积分商店**；**本站对一个号的责任止于「考察期结束、发完分」**，之后号的生死交给 cpa/cpamp。

---

## 1. 登录与身份

- Linux.do OAuth 登录。
- 信任等级门槛**后台可配**。
- 可后台切换为「**登录即可、不限信任等级**」。

---

## 2. 收号

### 2.1 类型
- ChatGPT(codex) / Claude(anthropic) / Grok(xai)。三类**写死可接受**；若实现代价小，做成可配置增减（**加分项，非必须**）。

### 2.2 交号方式
- codex：授权回调粘贴 + 直接粘贴 RT
- claude：授权回调粘贴
- grok：设备码轮询

### 2.3 账号唯一标识
- **唯一键 = `provider + 上游稳定 account_id`**。
- 邮箱 / auth file 文件名 / RT / 显示名称 **都不得**作为唯一键。
- **拿不到稳定 account_id 的号**（当前 claude/grok 很可能如此）→ **不自动发分**，进**人工复核**。codex 有深度巡检、能拿到，不受此限。

### 2.4 去重（一次性）
- 一个号（`provider+account_id`）**一辈子只能提交一次**：
  - **提交过 → 一律拒绝**，无积分，提示「这个号交过了，不能再交」——无论它当前在池、已失效、还是已被删；提交记录永久占用该唯一键。
  - 全新号（该唯一键从未出现过）→ 允许提交、入池。
- **掉号/失效 = 永久作废**，号主即使修好也不能重新提交。这是刻意取舍：换来 `account_id` 唯一键**一键同时保证「只交一次 + 只发一次」**，数据模型最简。

### 2.5 入池优先级
- 贡献号入池即设**高优先级，默认 10，后台可调**；cpamp 中**数字越大越优先调用**，考察期即生效。
- **考察通过后仍保持 10**（不恢复默认，可后台改）——贡献号多为临期号，确认健康后更该优先烧、把官方号池留着。

### 2.6 来源标记
- 入池的号在 cpamp 带来源标记（命名 hub 前缀 / 备注贡献者），号池侧一眼识别。

---

## 3. 考察期与积分

### 3.1 总原则（一号一次）
- 每个唯一号**一辈子只发一次分**，发给这唯一一次提交的贡献者（一号只能交一次，见 §2.4）。
- **contributions 一号一行、永不删除，它本身就是永久账本**——发分标记记在这行上，即使 cpamp 那边删了号也不复位。**不需要独立账本表**。
- 号提交后若没走到发分（硬失败判死），该号**永久作废**，不再有第二次机会（一号只能交一次）。
- 积分**永不收回**。

### 3.2 状态机（封死）

```
已提交 → 首检中 → 考察中 → { 已发分 / 已失败 / 待人工复核 }
```

- **已提交**：拿到凭证。
- **首检中**：验证凭证真实可用。**授权成功只代表拿到凭证，不代表套餐正确或真实请求可用**；**首检通过后才启用并设高优先级**。
- **考察中**：号启用、及时使用（高优先级烧），窗口 **T**（后台可调，默认 24h）。
- **终态**：
  - **已发分**：窗口内无硬失败 → 发那唯一一次分。
  - **已失败**：窗口内出现硬失败（判死）→ **永久作废**，该唯一键不能再交。
  - **待人工复核**：拿不到稳定唯一 ID，或系统无法判定。
- **发分判定细则**：
  - **硬失败**（任意时刻，以**系统实际观测并持久化的事件**为准）→ 判死、终结、**永久作废**。
  - **软失败不阻断发分**——被烧到限流/额度满，恰证明号真、已赚到。
  - **未知不算失败**：CPA/cpamp 网络故障、本站重启、worker 停机 = 未知，**不得**算硬失败。
- **故障顺延**：不可观测时段（worker/CPA 故障、本站重启）**暂停考察计时**，恢复观测后接着走。诚实代价：若号在不可观测期真死了，恢复后观测到死号 = 无法确认全程健康 = **判死不发分**（只在"正向确认持续健康"时才发）。
- 贡献者看到「验证中，预计 T 后发放积分」。

### 3.3 三家失败判定表

| 情况 | Codex | Claude / Grok |
|---|---|---|
| 401 / 撤权 / 需重授权 | 硬失败 | 硬失败 |
| 402 / 额度耗尽 | 软失败（不阻断发分） | 见下 \* |
| 429 / 限流 | 软失败（不阻断发分） | 见下 \* |
| 网络超时 / CPA 5xx | 未知，重试（暂停计时） | 未知，重试（暂停计时） |
| 管理员手动禁用 | 不算账号失败 | 不算账号失败 |

\* **claude/grok 读不到禁用原因的特殊规则**：cpamp 只给「启用/禁用」、拿不到原因。策略 = **复检一次，仍禁用即判失败**，不走人工。已知代价：被限流自动禁用的好号可能误杀——三家不对等的税，接受。

### 3.4 考察快照（防后台改配把正在考察的人搞懵）
- 号**进入考察那一刻**，冻结本次的：**考察时长 T、分值规则、检测规则版本、优先级**。
- 后台改 T / 分值 / 优先级 **只影响之后的新提交**；正在考察的按进入时的快照结算。

### 3.5 分值规则（维度 C，均后台可调）
- **codex 按套餐**：`pro=40 / plus=15 / team=25 / edu=20 / 兜底=10`
- **claude 按 provider 固定**：`20`
- **grok 按 provider 固定**：`15`
- 数字为占位，后台随改。

### 3.6 发分幂等
- 同一号发积分**必须幂等**：worker 重试/重入不得重复发分。（现有 `awardPoints` 已具备，**须保持**。）

---

## 4. 号的后半生（贡献者视角）

- **不支持主动撤回**——刻意的产品决策，防「捐号拿分后自己在网页端登出把号弄死」的恶意撤回。
- dashboard 给贡献者看**自己交过的号 + 当前状态（存活 / 已失效）**（查看时向 cpamp 拉一次**只读**状态，不算管理）。号失效即**永久作废**，不能重交（见 §2.4）。
- 本站**不主动推送**掉号通知。
- **不做风险警告披露页**——同为刻意决策，留痕于此。

---

## 5. 积分商店

### 5.1 商品与定价
- 商品**后台可增删改**、价格可改、**使用说明可编辑**、**上架/下架开关**、**限购次数按商品单独配**（如 VIP 每人限 1）。

### 5.2 商品模型（防止把「四类」写成四套逻辑）
- 永久额度 / 限时额度 / VIP / 邀请码这四个「类型」**只是展示分类**，仅决定图标与文案，**不产生逻辑分支**。
- 商品 = `{ 名称, 积分价格, 使用说明, CDK 库存, 每人限购, 上下架 }`，履约一律发 CDK。

### 5.3 履约（统一 CDK）
- 四类商品**统一发 CDK**；CDK **后台一键批量导入**、**库存制（兑一发一）**。
- 兑换成功发一个**可复制的 CDK**，**实际核销去主站 / 对应服务平台**，本站不做任何开通动作。
- **CDK 状态至少含：可用 / 已发放 / 作废**；批量导入**去重**。
- **CDK 不得出现在日志、排行榜或任何他人可见接口**。
- 响应丢失后，用户仍能在**兑换记录**里找回已领 CDK。

### 5.4 库存与限购
- 库存空 → **售罄（不扣分）**。
- 积分不足 → **拦截**并提示。
- 到限购上限 → **拦截**。

### 5.5 兑换原子交易
- 一次兑换 =「**检查余额 → 检查限购 → 占用 CDK → 扣积分 → 写兑换记录**」**同成功或同失败**。
- 用户重复点击 / 超时重试**不得**重复扣分或领两个 CDK（幂等）。
- （现有 `spendPoints` 条件原子扣已具备，**须保持**。）

---

## 6. 展示与记录

- **排行榜**：登录可见，显示 **Linux.do 用户名**（实名）；**按累计获得积分排名**（花掉不掉名次）。
- **重复提交拦截**：提交一个**交过的号**（该唯一键已存在）时，直接拒绝并告知「**这个号交过了，不能再交**」。
- **用户 dashboard**：我的号（+ 状态）、积分余额、**积分明细**（每笔加/扣：时间 / 原因 / 数额）、兑换记录（我换过的 CDK）。
- **管理后台**：看**贡献记录**（谁交了什么号 / 状态 / 发没发分）+ **兑换记录**（谁换了什么 / 发的哪个 CDK）。

---

## 7. 管理后台（权限 + 留痕）

### 7.1 可配项汇总
信任等级门槛 & 限身份开关 · 分值规则（codex 按套餐 / claude·grok 按 provider）· 考察窗口 T · 入池优先级数值 · 商品（增删改 / 价格 / 使用说明 / 限购 / 上下架）· CDK 批量导入。

### 7.2 管理员身份
- Linux.do ID 白名单 或 独立管理员角色。

### 7.3 操作留痕（审计）
- 改分值 / T / 商品 / 库存 / CDK / 优先级 → 记录**操作人 / 时间 / 旧值 / 新值**。

### 7.4 人工干预
- 提供卡住任务的**人工重试 / 终止**能力；但人工操作**不得绕过**「唯一号只发一次」。

---

## 8. 部署与非功能

- **全站界面中文**。
- 本站需**常驻 Node 进程**跑考察巡检 worker（serverless 不适用）。
- RT 不落本地库（换取后上传 cpamp），故「RT 加密存储」不适用。
- **缺 SESSION_SECRET / CPA 密钥，或仍 `MOCK=true` → 生产拒绝启动**（现有 env.ts fail-fast 已具备，**须保持**）。
- **RT / 管理密钥 / 完整 CDK 不写入应用日志**；CPA 报错不透传原文；admin 登录加限流。
