# 小鸡毛账号收集系统 · 需求文档（定稿 v4）

> 日期：2026-07-20　｜　状态：**需求锁定（v4）**。**v4 改动（发分模型换引擎）**：发分依据从「考察期健康判定→熬过窗口 T 发一次固定分」改为「**号在池期间按 cpamp 每日实际调用量折算积分（按次×单价）、按日结算、一号可持续发分**」。**考察期整个取消**（首检保留）。移除 v3 的「一号一辈子只发一次 / 终身账本 / 考察窗口 T / 故障顺延 / 软硬失败判死」整条线。新增商店「换 LDC」（走 CDK 式发码、每日限量）。涉 §0 / §2.4 / §3（整节）/ §4 / §5 / §7 / §8。
> **v4 相对 v3 为什么变**：老模型「熬窗口测活发固定分」有个真不公平的洞——号主的额度被大家烧光后，我们再测活反而 401，号主啥也拿不到。改「用了多少给多少」既公平、号也稳（能用就一直有价值），还省掉一整套考察期/故障顺延的复杂机制。
> 本文只描述**需求**，不涉及当前代码进度与实现方案。

---

## 0. 定位与边界

- **一句话**：贡献者交出自己的 AI 订阅账号 → 进我的 CPA(cpamp) 号池 → **号被调用产生用量** → **按日用量折算发积分** → 积分兑换 CDK / LDC。
- **边界（关键）**：号池的长期监控/管理是**另一个站**的事。本站只做**贡献台 + 积分商店**：**号在池期间，本站按日把它的实际调用量结算成积分发给号主；号哪天不能用了，那天起自然没用量、不再产生新积分**。之前已结算的积分永久有效。

---

## 1. 登录与身份

- Linux.do OAuth 登录。
- 信任等级门槛**后台可配**。
- 可后台切换为「**登录即可、不限信任等级**」。

---

## 2. 收号

### 2.1 类型
- ChatGPT(codex) / Claude(anthropic) / Grok(xai)。三类**写死可接受**；若实现代价小，做成可配置增减（**加分项，非必须**）。

### 2.2 交号方式
- codex：授权回调粘贴 + 直接粘贴 RT
- claude：授权回调粘贴
- grok：设备码轮询

### 2.3 账号唯一标识
- **唯一键 = `provider + 上游稳定 account_id`**。
- 邮箱 / auth file 文件名 / RT / 显示名称 **都不得**作为唯一键。
- **拿不到稳定 account_id 的号** → **不入池计量**，进**人工复核**（三家稳定 ID 字段已探明：codex=`chatgpt_account_id` / claude=`account` / grok=`sub`，正常都能拿到；拿不到＝残缺号，人工处理）。

### 2.4 去重（一次性）
- 一个**成功入池**的号（`provider+account_id`）**一辈子只能提交一次**：
  - **首检通过入池 → 占用唯一键**。之后再提交该号 → 一律拒绝，提示「这个号交过了，不能再交」——无论它当前在池、已失效、还是已被删。
  - 全新号（该唯一键从未成功入池过）→ 允许提交。
- **首检失败的号不占唯一键、可重交**：号若首检就 401/封号（压根没成功入池），退回、不保留、**允许用户修好后重新提交**（如重新授权）。只有**真正入池**的号才登记归属、才锁唯一键。
- **为什么入池后是「只交一次」**：防同一个能用的号被反复提交刷积分归属、防一号多主争抢。这和「按量持续发分」不冲突——号只登记一次归属，之后它产生的**所有**用量都按日结算给这唯一的号主。
- **入池后掉号/失效**：号不能用了就自然停止产生用量（那天起没分），号主之前赚的积分保留；**不支持修好后重新提交**（已锁唯一键）。

### 2.5 入池优先级
- 贡献号入池即设**高优先级，默认 10，后台可调**；cpamp 中**数字越大越优先调用**。
- 高优先级对号主**更有利**——号被优先烧＝调用量高＝积分多。贡献号多为临期号，优先烧掉、把官方号池留着，双赢。
- **为何不怕「自调自号刷分」**（决策留痕）：号池调用者**不能指定用哪个号**（cpamp 按路由策略自动选），且**单号本身有额度上限**（很快烧完）——想靠疯狂调用自己的号刷积分，既路由不可控、又受额度天花板卡死，刷不动。故不做额外防刷（用户 2026-07-20 判定）。

### 2.6 来源标记
- 入池的号在 cpamp 带来源标记（命名 hub 前缀 / 备注贡献者），号池侧一眼识别、也便于按号归集用量。

---

## 3. 计量与积分（v4 核心：按量计量，无考察期）

### 3.1 总原则（按量、按日、可持续）
- 号在池期间，**按它每日的实际调用量折算积分，按日结算，发给该号唯一的贡献者**（一号一主，见 §2.4）。
- **一号可持续发分**——不是「发一次就完」，是「今天用了多少、结算多少」，天天累积，直到号不能用为止。
- 积分**永不收回**。
- **数据源**：cpamp 的**用量 API**（`/v0/management/usage`，2026-07-20 实探确认：每条请求事件带 `account_snapshot`/`auth_file`/`provider`/`auth_label`/`timestamp`，能精确按号、按日归集调用次数）。这是唯一计量依据——真实用了多少就发多少，天然公平。
- **只算走 cpamp 号池的调用量**：号主自己直接开 ChatGPT/Claude 网页端用的，cpamp 根本不知道、不计入。号主要明白「交出来被大家烧才有分」。

### 3.2 状态机（v4 简化）

```
已提交 → 首检中 →（能用）在池计量中 →（号失效/下架）已停用
                 ↘（401/封号，不能用）首检失败·退回
                 ↘（拿不到稳定 ID/异常）待人工复核
```

- **已提交**：拿到凭证（此时**尚未**占用唯一键，见 §2.4）。
- **首检中**：确认号**能被调用**——即「能登录、没被封」。授权成功只代表拿到凭证、不代表能用。
  - **通过 = 能登录、未封号**（哪怕当前 5h/7天限额、额度暂时为 0 也算通过——反正调用上了才计分，先入池、等额度恢复）→ 入池、启用、设高优先级、开始计量、**此刻占用唯一键**。
  - **失败 = 401 不能登录 / 被封号** → **退回、告知用户「这号登录失败/被封了，没收」、不保留、不占唯一键**（用户修好可重交，见 §2.4）。
- **在池计量中**：号被调用；worker 按日采集用量 → 折算积分 → 发给号主。只要还能调用，就一直停这态、天天结算。
- **已停用**：号**明确失效**（凭证撤销/封号/被删）或管理员下架 → 停止计量。之前积分保留；不复活、不能重交。
- **待人工复核**：拿不到稳定唯一 ID，或系统无法判定。

> **「限额不算失败」是关键**：号被烧到 5h/7天限额、额度暂时为 0 ≠ 号死了——它还在池里、等额度恢复就继续被调用、继续计分。只有**明确不能登录/被封**才判首检失败或停用。

> **对比 v3 去掉了什么**：考察窗口 T、「熬过 T 发固定分」、软/硬失败判死、故障顺延（暂停计时）、考察快照冻结、「一号只发一次」——按量模型下这些都不需要（用了多少给多少本身就公平；停机那天就是没用量、无需补偿）。

### 3.3 按日结算（幂等）
- **结算单位 = (号, 日期)**：每号每天结算一笔，按当日调用量折算积分。
- **幂等**：`(号, 日期)` 唯一，worker 重跑/重启不得对同一号同一天重复发分。
- **结算时机（默认，后台可调）**：每天固定时刻结算前一自然日用量（默认每日 00:10，时区随服务器）。号首检入池当天即可开始计量（当日不足整天按实际用量算）。

### 3.4 折算规则（已定：按次×单价，后台可配）
- **积分 = 当日调用次数 × 每次积分单价**。cpamp 请求监控给的是**调用次数**，直接乘单价，简单直观。
- 单价**后台可配、可按 provider/套餐分档**（如 codex-pro 每次 X 分、claude 每次 Y 分）；数字全为占位、随时调。
- 改动**只影响之后的结算，不追溯已结算的日**（每日结算即定，历史不重算）。
- （「1207 次 ≈ 7 美金」只是号主的直观感受；内部一律以积分计。）

### 3.5 号存活与停止
- 号在池期间**不需要专门「判死」**：能用就有调用量、就有分；不能用就没量、自然停。
- **失效识别（默认）**：靠 cpamp 巡检/监控的号状态——**明确失效（凭证撤销/封号/删除）即标「已停用」、停止采集**；**单纯零用量不算停**（可能只是那天没被调，号还活着）。判停策略后台可细调。
- **三家一视同仁**：codex/claude/grok 统一「能调用即计量、明确失效才停」。不再对 claude/grok 搞 v3 的「复检一次判死」特殊规则（v4 无判死概念——号能调用就有分，不能调用就自然没量）。
- dashboard 查看时向 cpamp 拉一次**只读**状态显示「在用 / 已失效」。

---

## 4. 号的后半生（贡献者视角）

- **不支持主动撤回**——刻意的产品决策，防「捐号拿分后自己在网页端登出把号弄死」。（按量模型下恶意撤回的动机更小：号活着才持续有分，弄死自己号＝断自己财路。）
- dashboard 给贡献者看**自己交过的号 + 当前状态（在用 / 已失效）+ 累计为该号赚的积分**。
- 本站**不主动推送**掉号通知。
- **不做风险警告披露页**——刻意决策，留痕于此。

---

## 5. 积分商店

### 5.1 商品与定价
- 商品**后台可增删改**、价格可改、**使用说明可编辑**、**上架/下架开关**、**限购次数按商品单独配**。

### 5.2 商品模型（防止把「类型」写成多套逻辑）
- 永久额度 / 限时额度 / VIP / 邀请码 / **LDC** 这些「类型」**只是展示分类**，仅决定图标与文案。
- 商品 = `{ 名称, 积分价格, 使用说明, 履约方式, 每人限购, 上下架 }`。

### 5.3 履约
- **CDK 类**（额度/订阅/邀请码等）：统一发 CDK；后台一键批量导入、库存制（兑一发一）；发一个可复制 CDK，核销去主站/对应平台；CDK 状态含 可用/已发放/作废；导入去重；CDK 不进日志/排行榜/他人可见接口；响应丢失可在兑换记录找回。
- **LDC 类（新增，已定：走 CDK 式发码）**：合伙人的 linux.do 币，让 LDC 流通、捐号者能赚 LDC。
  - **复用现有 CDK 履约**：后台批量导入 LDC 凭证/码（一码一个 LDC 面额），兑换即发一个可复制码，核销去 linux.do；库存制、去重、不进日志——与 CDK 类完全同一套机制，只是展示分类标 LDC。
  - **每日限量**（默认 2000 LDC/日，后台可配）、按日重置额度；当日额度用尽 → 今日已抢完（不扣分）。
  - （未来若 linux.do 有转账 API 可升级为自动打款，本单不做。）

### 5.4 库存与限购
- 库存空（或当日 LDC 额度用尽）→ **售罄/今日已抢完（不扣分）**。
- 积分不足 → **拦截**并提示。
- 到限购上限 → **拦截**。

### 5.5 兑换原子交易
- 一次兑换 =「**检查余额 → 检查限购/当日额度 → 占用库存 → 扣积分 → 写兑换记录**」**同成功或同失败**。
- 重复点击 / 超时重试**不得**重复扣分或多领（幂等）。

---

## 6. 展示与记录

- **排行榜**：登录可见，显示 **Linux.do 用户名**；**按累计获得积分排名**（花掉不掉名次）。
- **重复提交拦截**：提交交过的号 → 拒绝并告知「**这个号交过了，不能再交**」。
- **用户 dashboard**：我的号（+ 状态 + 该号累计积分）、积分余额、**积分明细**（每笔加/扣：时间 / 原因〔含「X 号 M 月 D 日用量结算」〕/ 数额）、兑换记录。
- **管理后台**：看**贡献记录**（谁交了什么号 / 状态 / 累计发分）+ **每日用量结算记录** + **兑换记录**。

---

## 7. 管理后台（权限 + 留痕）

### 7.1 可配项汇总
信任等级门槛 & 限身份开关 · **折算规则（按次/按成本、单价、分档）** · **按日结算参数（时机/时区）** · 入池优先级数值 · **LDC 每日限量** · 商品（增删改 / 价格 / 使用说明 / 限购 / 上下架）· CDK 批量导入。

### 7.2 管理员身份
- Linux.do ID 白名单 或 独立管理员角色。

### 7.3 操作留痕（审计）
- 改折算规则 / 结算参数 / 优先级 / 商品 / 库存 / CDK / LDC 额度 → 记录**操作人 / 时间 / 旧值 / 新值**。

### 7.4 人工干预
- 提供卡住任务的**人工重试 / 终止**能力；人工操作**不得绕过**「同一号同一天只结算一次」的幂等。

---

## 8. 部署与非功能

- **全站界面中文**。
- 本站需**常驻 Node 进程**跑 **①首检 worker ②按日用量结算 worker ③号状态巡检**（serverless 不适用）。
- RT 不落本地库（换取后上传 cpamp），故「RT 加密存储」不适用。
- **缺 SESSION_SECRET / CPA 密钥，或仍 `MOCK=true` → 生产拒绝启动**（现有 env.ts fail-fast 已具备，须保持）。
- **RT / 管理密钥 / 完整 CDK 不写入应用日志**；CPA 报错不透传原文；admin 登录加限流。
- 部署仍钉死**单机单实例 + SQLite + 单 worker**（多实例须换 PostgreSQL + worker 独立进程，见路线图）。
